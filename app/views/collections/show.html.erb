<%= @collection.title %>
<br>
<%= @collection.description %>

<br><br>

<%= link_to "Edit", edit_collection_path(@collection) %>
<%= link_to "Delete", collection_path(@collection), method: :delete, data: { confirm: "Are you sure you want to delete this Collection?"} %>